# Rubric Review — heavy-log-streamer

**Generated:** 2026-06-26 12:21:16 UTC
**Model:** claude-opus-4-8
**Effort:** max
**Rubric:** task-implementation.toml (25 criteria)

---

I've read all task files and verified the oracle's algorithm against the test's expected values by hand (windows, error counts, nearest-rank p95, and the round-half-up boundary cases like 5002/40000=0.12505→0.1251 and 376/3000→0.1253). The execution sandbox blocks writes to `/app` and ad-hoc runs, but the data generators are deterministic and the math is fully traceable. Here is my per-criterion assessment.

[PASS] verifiable: Verification is fully programmatic and deterministic — `tests/test_outputs.py:14-149` generates fixed stress datasets in code (no randomness), runs the agent's `/app/aggregator.py`, and asserts exact metric values plus an RSS bound (`tests/test_outputs.py:197-209`). No LLM-judge is used; `tests/test.sh:5-11` maps pytest exit to a 0/1 reward. The deterministic generators and wide memory margin make re-runs reliable.

[PASS] solvable: `solution/solve.py` is a complete streaming implementation (bounded deques + per-ms histograms, `solve.py:31-96`) and I confirmed every hardcoded expected value matches its algorithm (e.g., auth window 1668 errors → 0.1668 at `test_outputs.py:201`; global 5002/40000 → 0.1251 at line 199; nearest-rank rank 9500 lands on latency 950). `solution/solve.sh:3-5` installs and runs it. Consistent with the 2.0h expert estimate.

[PASS] difficult: Multiple non-obvious correctness traps compound: Python's `bool`-is-`int` subclass must be explicitly excluded (`solve.py:57,60`), JSON floats like `200.0` rejected, exact-key-set enforcement, nearest-rank percentile via `ceil` (`solve.py:14`), and round-half-up vs Python's banker's `round()` — the 0.12505 case (`test_outputs.py:199`) specifically punishes naive `round()`. Combined with sliding-window bookkeeping under a memory bound, a competent senior engineer would find this non-trivial.

[PASS] interesting: A memory-bounded log stream processor computing sliding-window error rates and p95 latency is a realistic observability/SRE problem someone would be paid to build. The OOM-on-large-files framing (`instruction.md:1`) is a genuine production motivation.

[PASS] outcome_verified: Grading is outcome-based — it executes the artifact and checks `output.json` values and peak RSS, not the approach. The only approach-touching check is a source scan forbidding `pandas`/`numpy` (`test_outputs.py:168-173`), which is a legitimate mechanistic enforcement of the stated stdlib-only constraint rather than a tool mandate, and is dwarfed by the functional assertions.

[PASS] anti_cheat_robustness: Each test removes `output.json` and regenerates hidden input the agent never sees (`test_outputs.py:182-185,217-221,249-254`), and three structurally distinct datasets (large/short/empty) require a real implementation — a stub or stale-output cheat cannot satisfy all three. There is no byte-identical fixture overlap (data is generated in-code; the 3-line seed `environment/data/system.log` is unused by tests). A theoretical psutil-tamper vector exists since `test.sh:3` may skip reinstall if already satisfied, but it cannot bypass the exact-value correctness asserts, so it yields no illegitimate pass.

[PASS] task_security: All files perform only task-relevant actions — Dockerfile installs python3/pip and copies the aggregator+data (`environment/Dockerfile:5-11`), `test.sh` pip-installs pinned psutil/pytest, `solve.sh` copies and runs the solution. No exfiltration, obfuscation, network calls beyond PyPI, or prompt injection; the Portuguese comments are benign.

[PASS] functional_verification: Verification primarily executes the agent's program as a subprocess and inspects real outputs and measured RSS (`test_outputs.py:151-166,187-209`). The two `assert "pandas/numpy" not in content` lines are a minor supplement, not the basis of grading, so this is behavior-driven verification.

[PASS] verifier_reward_surface_check: The verifier exercises the artifact against three hidden, engineered datasets with boundary distributions the agent cannot anticipate (e.g., the 946–954 latency ramp at `test_outputs.py:73-93` placing the p95 rank exactly on 950), checking precise windowed metrics rather than artifact shape. A plausible stub that prints fixed values or echoes input cannot pass, since input is regenerated and output is removed before each run.

[PASS] deterministic_reproducible: Test deps are pinned (`test.sh:3`, psutil==6.1.1, pytest==8.3.4), datasets are generated deterministically with no RNG, and there are no live-service dependencies beyond pinned PyPI installs. The oracle uses ~20–35MB against a 64MB ceiling, so the memory assertion (`test_outputs.py:209`) has a wide, stable margin across runs.

[PASS] essential_difficulty: Difficulty stems from genuine reasoning — type-strict schema validation, sliding-window state maintenance, nearest-rank percentile, and exact rational rounding — not from formatting or arbitrary precision. The output is a simple flat 10-key JSON; the precision requirements (round-half-up) are conceptual algorithm choices explicitly specified in `instruction.md:10`, not clerical.

[PASS] test_instruction_alignment: Coverage is strong both ways: all 10 malformed traps (`test_outputs.py:36-53`) map to the schema rules in `instruction.md:3`, windows/rounding/partial-window/empty cases all trace to `instruction.md:5-10`, and every assertion traces back to a stated requirement. One point to flag: the instruction says "Ignore blank lines" while generators emit whitespace-only blanks `"    \n"` (`test_outputs.py:28`) that must be treated as blank (not malformed) — the conventional engineering reading of "blank line" includes whitespace-only, so this aligns, but a stricter reading could diverge.

[PASS] novel: This is a custom combination (streaming + multi-window sliding metrics + strict primitive-type JSON contract + nearest-rank p95 + round-half-up + RSS bound) rather than a standard textbook exercise, and the bug-laden seed `aggregator.py` requires genuine exploration rather than recall.

[PASS] agentic: The task requires inspecting the failing `environment/aggregator.py`, understanding its OOM/correctness defects, and iterating a refactor against memory and exact-value constraints — multi-step environment interaction that cannot be one-shot generated reliably.

[PASS] reviewable: A non-specialist can verify correctness because the deterministic generators are explicit, the oracle independently computes the same values, and one derivation comment is shown (`test_outputs.py:233`). The expected metrics are hardcoded literals rather than computed in-test (a mild red flag), but the provided oracle plus readable generators give a clear verification path.

[PASS] instruction_concision: `instruction.md` is three tight paragraphs plus a bullet list, uses absolute paths throughout (`/app/aggregator.py`, `/app/data/system.log`, `/app/output.json`), backticks identifiers, states the goal not a procedure, and has no AI preamble or tool enumeration. It is dense but proportionate to the task's complexity.

[PASS] structured_data_schema: The exact output schema is normatively documented in `instruction.md:5-10` — the four windows, the `{window}_error_rate` (4 dp) and `{window}_p95_latency_ms` (2 dp) naming, `processed_count`/`malformed_count`, flat JSON, and "exactly these keys" — and the test enforces the 10-key set (`test_outputs.py:196`).

[PASS] typos: Paths and identifiers are consistent across `instruction.md`, `Dockerfile:10-11`, `solve.py`, `solve.sh:3`, and the tests (`/app/aggregator.py`, `/app/data/system.log`, `/app/output.json`); the output keys match between `solve.py:106-124` and the assertions. No typos in critical identifiers were found.

[PASS] category: `task.toml:2` sets `category = "software_engineering"`, which accurately describes refactoring a stream processor for memory and correctness.

[PASS] task_name: `heavy-log-streamer` is lowercase kebab-case, three words, 18 characters, and clearly conveys the task's subject; it is well within the ≤5-word / <30-char limits.

[PASS] resource_configuration: Values are deliberately set, not defaults — verifier 600s and agent 18000s suit a hard refactor, cpus=2/memory_mb=4096 give development headroom while the 64MB limit is enforced by the test rather than the container (`task.toml:5-16`). Difficulty comes from reasoning, not compute; `build_timeout_sec=6000` is generous for a small apt install but harmless.

[PASS] expert_time_estimate: `task.toml:3` sets 2.0 hours, which is plausible for implementing streaming sliding-window aggregation with strict validation, nearest-rank p95, and round-half-up — consistent with the task's apparent complexity and the generous agent timeout.

[PASS] task_toml_schema: All fields are valid Harbor fields — `[metadata]` has only `category`/`expert_time_estimate_hours`, `[verifier]`/`[agent]` use `timeout_sec`, and `[environment]` uses `build_timeout_sec`, `cpus`, `memory_mb`, `storage_mb`, `gpus`, `allow_internet` (`task.toml:1-17`). No invented fields are present.

[PASS] solution_quality: `solve.sh:3-4` copies a real 130-line `solve.py` file (not an inlined heredoc) and executes it; the solution performs genuine streaming computation (`solve.py:45-96`) rather than echoing precomputed answers. Keeping the implementation in its own file aids review.

[PASS] environment_hygiene: The Dockerfile copies only `aggregator.py` and `data` — no `tests/` or `solution/` (`Dockerfile:10-11`) — and test-only deps psutil/pytest are installed in `test.sh:3`, not baked into the image. Apt uses `apt-get update` with `rm -rf /var/lib/apt/lists/*` cleanup and no version pins (`Dockerfile:5-8`); `python3-pip` in the image is a general tool used by the verifier, a minor point.

TOTAL: 25 PASS, 0 FAIL, 0 N/A
